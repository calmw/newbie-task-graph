{
  "name": "statistics-subgraph",
  "license": "MIT",
  "scripts": {
    "build": "graph codegen && graph build",
    "create-dev": "graph create into-newbie-task --node http://127.0.0.1:8020",
    "deploy-dev": "graph deploy into-newbie-task --ipfs http://localhost:5001 --node http://127.0.0.1:8020/",
    "remove-dev": "graph remove into-newbie-task --node http://localhost:8020/",
    "create-main": "graph create into-newbie-task --node http://127.0.0.1:6020",
    "deploy-main": "graph deploy into-newbie-task --ipfs http://localhost:6001 --node http://127.0.0.1:6020/",
    "remove-main": "graph remove into-newbie-task --node http://localhost:6020/"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.49.0",
    "@graphprotocol/graph-ts": "^0.29.1"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
·